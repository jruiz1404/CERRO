<core:FragmentDefinition
  xmlns="sap.m"
  xmlns:core="sap.ui.core"
  xmlns:html="http://www.w3.org/1999/xhtml"
>

  <Dialog id="_IDGenDialog"
    title="{ parts: ['i18n>titleScanDialog', 'scanModel>/type'],
            formatter: 'jQuery.sap.formatMessage'}"
    showHeader="true"
    class="sapUiContentPadding"
  >
    <content>
      <Input id="_IDGenInput"
        value="{scanModel>/value}"
        placeholder="{i18n>placeholderInput}"
      />
      <Text id="_IDGenText" text="{i18n>textInputDialog}" />
    </content>
    <beginButton>
      <Button id="_IDGenButton"
        text="{i18n>textOkButton}"
        icon="sap-icon://accept"
        press="onOkPress"
      />
    </beginButton>
    <endButton>
      <Button id="_IDGenButton1"
        text="{i18n>textCancelButton}"
        icon="sap-icon://decline"
        press="onCancelPress"
      />
    </endButton>
  </Dialog>
</core:FragmentDefinition>