# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: cerro.dsi
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: http://http://10.150.1.3:8000/
          client: '100'
        app:
          name: ZDSI_CORDOBA
          package: ZDSI
          transport: CNDK907806
        exclude:
          - /test/
